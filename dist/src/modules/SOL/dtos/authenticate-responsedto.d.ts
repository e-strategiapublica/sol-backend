export declare class AuthenticateResponseDto {
    email: string;
    name: string;
    id: string;
    token: string;
    refreshToken: string;
    type: string;
    roles: string;
    constructor(email: string, name: string, id: string, token: string, refreshToken: string, type: string, roles: string);
}
